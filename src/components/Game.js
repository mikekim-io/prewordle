import React from 'react';
import Board from './Board';
import Keyboard from './Keyboard';

export const Game = (props) => {
  return (
    <>
      <Board />
      <Keyboard />
    </>
  );
};

export default Game;
